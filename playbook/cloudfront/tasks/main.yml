---
- block:
    - name: "{{ aws_type[4] | default('') }} | Terraform Plan"
      terraform:
        project_path: "/root/mzc-aws-provisioning/terraform/{{ aws_type[4] | default('') }}/"
        plan_file: terraform.plan
        state: planned

    - name: "{{ aws_type[4] | default('') }} | Terraform Apply"
      terraform:
        project_path: "/root/mzc-aws-provisioning/terraform/{{ aws_type[4] | default('') }}/"
        state: present

  rescue:
    - name: "{{ aws_type[4] | default('') }} | Terraform Failure Message"
      debug:
        msg: '################### FAILED ###################'

